import { getAlphaChannelType } from './channels.js'

export default async function (bot) {
  // const guild = bot.guilds.cache.get(`711043006253367426`),
  //   mediaChannel = guild.channels.cache.get(`1123432202206707915`),
  //   alphaChannelType = getAlphaChannelType(mediaChannel)
  // console.log(mediaChannel?.name, alphaChannelType)
}
