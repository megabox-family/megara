import { getThreadById } from './threads.js'

export default async function (bot) {
  // const guild = bot.guilds.cache.get(`711043006253367426`),
  //   channel = guild.channels.cache.get(`1150588258422636546`),
  //   thread = await getThreadById(channel, `1150590323932807258`)
  // console.log(thread)
}
